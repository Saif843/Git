console.log(parseInt(Math.random() * 100 + 1))


const ele = document.createElement('p')

// selection
// number of turns
//array to dispay the player there previous numbs/failed guess
//empty the currentnumbers and put the new one of no guess is passed
//and number of turns is availables



let playGame = true

const form = document.querySelector('form')

form.addEventListener('submit', function (e) {
    e.preventDefault()
})
